#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @: The character to check
 *
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

	return (0);
}
